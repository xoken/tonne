import reducer from './claimTwitterHandleReducer';
import * as actions from './claimTwitterHandleActions';
import service from './claimTwitterHandleService';

export default { reducer, actions, service };
