import reducer from './mailReducer';
import service from './mailService';
import * as selectors from './mailSelectors';
import * as actions from './mailActions';

export default { reducer, actions, selectors, service };
