import reducer from './allpayReducer';
import * as actions from './allpayActions';
// import * as selectors from './allpaySelectors';
import service from './allpayService';

export default { reducer, actions, /*selectors,*/ service };
