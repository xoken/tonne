import reducer from './walletReducer';
import service from './walletService';
import * as selectors from './walletSelectors';
import * as actions from './walletActions';

export default { reducer, actions, selectors, service };
